<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
            {{ __('Representation') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900 dark:text-gray-100">
                    <h1>Representations</h1>
                    <ul>
                        @foreach($representations as $representation)
                            <li>
                                <a href="{{ route('representation.show', $representation->id) }}">{{ $representation->show->title }} - {{ $representation->show->location->locality->postal_code }} - {{ $representation->show->location->locality->locality }} - {{ $representation->schedule }}</a>
                            </li>
                        @endforeach
                    </ul>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
